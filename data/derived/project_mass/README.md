# Project Mass

Derived tables for the `project mass` block.

Files:

- `project_mass_summary.csv`
  - compact summary of the analyzed folder snapshot
- `project_mass_daily.csv`
  - daily accumulation across the wider project folder
- `project_mass_toe_daily.csv`
  - daily accumulation restricted to TouchDesigner `.toe` files
